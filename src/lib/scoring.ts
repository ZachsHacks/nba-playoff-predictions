import { SupabaseClient } from "@supabase/supabase-js";

export async function scoreSeries(supabase: SupabaseClient, seriesId: string) {
  // Stub -- will be implemented in Task 13
  console.log(`Scoring series ${seriesId} (stub)`);
}
